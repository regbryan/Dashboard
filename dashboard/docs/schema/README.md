# Database schema

Generated from the live Supabase project (`oqzkwsqrxpqntlojsvzz`). 13 tables in `public`. All RLS-enabled. See each table's purpose below the diagram.

## ER diagram

```mermaid
erDiagram
    brands ||--o{ posts : "has many"
    brands ||--o{ brand_logos : "has many"
    brands ||--o{ user_brand_access : "owners + clients"
    brands ||--o{ script_runs : "job history"
    posts ||--o{ approvals : "review trail"
    posts ||--o{ script_runs : "per-post jobs"
    brand_kits ||--o{ brand_kit_assets : "logos / preview images"
    brand_kits ||--o{ brand_kit_invites : "self-serve seats"
    brand_kits ||--o{ content_drafts : "approval queue"
    auth_users ||--o{ user_brand_access : "membership"
    auth_users ||--o{ pending_signups : "stripe → user link"

    brands {
        text id PK "slug"
        text name
        text handle "@instagram"
        text platform "instagram / linkedin / fb / tiktok"
        text subscription_status
        text subscription_tier "starter / growth"
        text stripe_customer_id
        text stripe_subscription_id
        text socialpilot_account_id "FK to SP profile"
        jsonb publishing_overlays "auto-applied before publish"
        text compliance "caption footer text"
    }

    posts {
        int id PK
        text brand_id FK
        int post_number
        text date "ISO yyyy-mm-dd"
        text post_type "Image / Reel / Text Post"
        text content_pillar
        text concept "prompt-preview text"
        text caption
        text hashtags
        text visual_direction
        text status "not_started / generating / in_review / approved / scheduled / posted"
        text file_path "post-images bucket key"
        text socialpilot_queue_status "queued / failed / published / disabled"
        int socialpilot_retry_count
        jsonb image_brief "structured Gemini brief"
    }

    approvals {
        int id PK
        int post_id FK
        text status "approved / changes_requested"
        text comment
        timestamptz notified_at
    }

    user_brand_access {
        uuid id PK
        uuid user_id FK "auth.users"
        text brand_id FK
        text role "client / admin"
    }

    brand_logos {
        uuid id PK
        text brand_id FK
        text label
        text storage_path "post-images bucket key"
        bool is_default
    }

    script_runs {
        int id PK
        text script_name
        text brand_id FK
        int post_id FK "nullable"
        text status
        text output
    }

    brand_kits {
        uuid id PK
        text slug UK
        text name
        text tagline
        text positioning
        text mission
        text archetype "Jungian — Hero, Sage..."
        text industry
        jsonb colors
        jsonb fonts
        jsonb tone
        jsonb content_pillars
        jsonb audiences
        text photography_direction
        text onboarding_status
        text review_status
        text tier
    }

    brand_kit_assets {
        uuid id PK
        uuid brand_kit_id FK
        text kind "logo / ig_post / hero_image / cutout / preview"
        text url
        text status "pending / approved / changes_requested"
    }

    brand_kit_invites {
        uuid id PK
        text slug
        text email
        text jti UK "JWT id"
        timestamptz expires_at
        timestamptz used_at
        uuid used_brand_kit_id FK
        text source "admin / self-serve"
    }

    content_drafts {
        uuid id PK
        uuid brand_kit_id FK
        date slot_date
        text platform
        text pillar
        text caption
        text image_url
        text status
    }

    pending_signups {
        uuid id PK
        text email
        text tier
        text stripe_session_id UK
        text stripe_customer_id
        text stripe_subscription_id
        uuid claimed_by_user_id FK "auth.users"
        text claimed_brand_id
    }

    ig_profile_cache {
        text handle PK
        jsonb payload
        timestamptz fetched_at
    }

    socialpilot_credentials {
        smallint id PK "= 1, singleton"
        text refresh_token
        text access_token
        timestamptz access_token_expires_at
        text last_error
    }
```

## Table reference

### `brands`
The public-facing identity. `id` is a slug (`iec`, `omega`, `csc`). One row per active customer brand. Holds subscription state (Stripe customer, tier, period end), SocialPilot binding, and publishing-overlay rules. 8 rows currently.

### `posts`
Every scheduled or generated post. FK to `brands`. Generated by autopilot cron from a seeded calendar at onboarding. Caption + image_brief + file_path get populated by the Gemini pipeline. SocialPilot fields track auto-queue state. 87 rows currently.

### `approvals`
Append-only log of client decisions on a post. `status` is `approved` or `changes_requested`. `comment` holds the client's note. Drives the change-request loop back into `generating`.

### `user_brand_access`
Cross-table that grants a Supabase Auth user (`auth.users`) access to a brand with a role. Created during onboarding's atomic `provision_brand` RPC.

### `brand_logos`
One brand can have many logo variants (`4C`, `1C`, white-on-dark, etc.). `is_default` picks the hero logo. `storage_path` is the post-images bucket key. `local_path` is the dev-only filesystem path used by Python overlay scripts.

### `script_runs`
Job history for shell scripts (logo overlay, footer overlay, hyperframes render). `output` holds stdout/stderr. Used by the dashboard to show "last ran" status.

### `brand_kits`
Separate from `brands` — this is the **derived** kit (positioning, mission, archetype, palette, pillars). Populated either by manual onboarding form or by the `derive-kit` autopilot reading approved posts over time.

### `brand_kit_assets`
Per-kit assets: logos, IG post samples, hero images, cutouts. Used by `derive-kit` for visual inference. Each has its own review status.

### `brand_kit_invites`
JWT-signed invite tokens for self-serve onboarding (separate from the Stripe-driven funnel via `pending_signups`).

### `content_drafts`
Older draft queue table — being superseded by `posts`. Retained for legacy compatibility but new flows write to `posts`.

### `pending_signups`
Staging table between Stripe checkout (no user_id yet) and first sign-in. Webhook upserts here on `checkout.session.completed`; the user's first dashboard visit claims the row and copies tier + customer_id onto the new brand row.

### `ig_profile_cache`
Cache for Instagram profile data fetched during onboarding / kit derivation. Keyed by handle.

### `socialpilot_credentials`
Singleton row (id=1) holding the agency SocialPilot OAuth refresh + access tokens. **Dormant** — Postiz / direct SocialPilot integration is currently parked.
